# === HTTP 直连型核心依赖（必须安装）===
pandas>=1.5.0
requests>=2.28.0
rich>=13.0.0

# === 库封装型核心依赖（建议安装）===
akshare>=1.12.0
efinance>=0.5.0
yfinance>=0.2.0
thsdk>=1.0.0
mootdx>=1.0.0

# === 库封装型可选依赖（按需安装）===
# pysnowball>=0.1.0     # 雪球组合/北向资金，需要 xq_a_token Cookie
# baostock>=0.8.8       # AKShare 挂了备用

# === 可选增强层（检测到环境变量时建议安装）===
# tushare>=1.3.0
# alpaca-trade-api>=3.0
# finnhub-python>=2.4
# fmpsdk>=1.0
# tiingo>=0.15
# httpx>=0.24.0         # 东方财富妙想、金融界等使用

# === 开发依赖 ===
pytest>=7.0.0
pytest-cov>=4.0.0
tabulate>=0.9.0         # Markdown 表格输出增强
